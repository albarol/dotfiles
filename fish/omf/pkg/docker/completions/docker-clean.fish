complete -c docker-clean -a "week" -d "Remove containers not used on last weeks"
complete -c docker-clean -a "year" -d "Remove containers not used on last years"
complete -c docker-clean -a "month" -d "Remove containers not used on last months"
