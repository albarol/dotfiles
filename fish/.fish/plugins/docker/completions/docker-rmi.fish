complete -f -c docker-rmi -a 'minutes' -d 'Remove images used in last minutes'
complete -f -c docker-rmi -a 'days' -d 'Remove images used in last days'
complete -f -c docker-rmi -a 'weeks' -d 'Remove images used in last weeks'
complete -f -c docker-rmi -a 'months' -d 'Remove images used in last months'
